import LoginLayoutWrapper from "@/app/components/login/layout/LoginLayoutWrapper";

export default function LoginLayout({ children }) {
  return <LoginLayoutWrapper>{children}</LoginLayoutWrapper>;
}
